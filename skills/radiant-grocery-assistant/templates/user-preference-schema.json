{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GrocerySkillUserPreference",
  "type": "object",
  "properties": {
    "sale_bias": {
      "type": "string",
      "enum": ["yes", "no", "only-if-similar-quality"],
      "description": "User preference for sale-aware meal planning"
    },
    "preferred_stores": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "required": ["sale_bias"]
}
